const Footer = () => {
    return (
        <footer className="w-full text-center py-4 px-6 text-xs text-text-muted backdrop-blur-xs mt-auto">
            Data for weekly events provided by league partners and data for other events fetched from pokedata.ovh
        </footer>
    );
};

export default Footer;
