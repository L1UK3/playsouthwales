
let leagues = [];
let leagueFilters = {game: null};


async function loadLeagues() {
    // fetches the list of leagues and their info and updates the global leagues array
    return
}

function renderLeagueView() {
    return
}
